# AWS Configuration
AWS_PROFILE=default
AWS_REGION=ap-south-1

# SSM Parameter Paths (set these in AWS SSM before deploying)
# aws ssm put-parameter --name "/deadlineai/anthropic/api-key" --type SecureString --value "YOUR_KEY"
# aws ssm put-parameter --name "/deadlineai/twilio/account-sid" --type SecureString --value "YOUR_SID"
# aws ssm put-parameter --name "/deadlineai/twilio/auth-token" --type SecureString --value "YOUR_TOKEN"
# aws ssm put-parameter --name "/deadlineai/twilio/whatsapp-from" --type String --value "whatsapp:+14155238886"

# Local development only (not used in Lambda)
TABLE_NAME=deadlineai
BUCKET_NAME=deadlineai-pdfs
